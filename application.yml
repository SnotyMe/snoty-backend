environment: DEVELOPMENT
port: 8080
publicHost: http://localhost:{{port}}
featureFlags:
  adapter: flagd
database:
  adapter: sql
authentication:
  adapter: keycloak
redis:
  url: "redis://localhost:6379"
