# Snoty Changelog
Run 
```sh
./generate-changelog.sh <last-tag>..HEAD <new-tag>
```
to generate a new changelog entry. \
It will be printed to stdout; pipe into `wl-copy` to copy to clipboard or redirect to a file.
